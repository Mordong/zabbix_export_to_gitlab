# Минимальные зависимости — только PyYAML для парсинга/сравнения YAML.
# HTTP-клиенты намеренно реализованы на urllib (стандартная библиотека),
# чтобы не вносить лишних зависимостей в Airflow worker.
PyYAML>=6.0

# Если запускать DAG из этого пакета:
# apache-airflow>=2.7.0  # обычно уже стоит на воркере, не ставим из этого requirements
